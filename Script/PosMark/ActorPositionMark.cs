using UnityEngine;

public class ActorPositionMark : MonoBehaviour
{
    public string direction;
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    public string getDirection() {
        return direction;
    }
}
